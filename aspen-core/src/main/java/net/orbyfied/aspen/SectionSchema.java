package net.orbyfied.aspen;

/**
 * A schema for section classes.
 *
 * @author orbyfied
 */
public class SectionSchema extends Schema {

    public SectionSchema(Schema parent,
                         String name,
                         Object instance) {
        super(parent, name, instance);
    }

}
